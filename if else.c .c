#include<stdio.h>

int main()
{
    int a,b;
    printf("enter the a,b values :");
    scanf("%d%d",&a,&b);
    if(a>b)
    printf ("a is big number");
    else
    printf ("b is big number");
    return 0;
}